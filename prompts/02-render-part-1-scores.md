Render every MusicXML score in `scores/part-1` to a PDF using the installed
MuseScore command-line application. Save each PDF beside its corresponding
MusicXML file, retaining the base filename. Then concatenate the five PDFs in
numeric exercise order (1.1 through 1.5) into a single PDF named
`scores/part-1/part-1-complete.pdf`. Verify that the combined document opens
correctly and contains all rendered score pages.
